apiVersion: v1
kind: Service
metadata:
  name: ekstraksi
  labels:
    app.kubernetes.io/name: ekstraksi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ekstraksi
  ports:
    - name: http
      port: 8030
      targetPort: http
