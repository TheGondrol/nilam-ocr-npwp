apiVersion: v1
kind: Service
metadata:
  name: scoring # nama ini adalah alamatnya: http://scoring:8033 di dalam namespace
  labels:
    app.kubernetes.io/name: scoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: scoring
  ports:
    - name: http
      port: 8033
      targetPort: http
