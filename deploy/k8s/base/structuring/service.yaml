apiVersion: v1
kind: Service
metadata:
  name: structuring # nama ini adalah alamatnya: http://structuring:8032 di dalam namespace
  labels:
    app.kubernetes.io/name: structuring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: structuring
  ports:
    - name: http
      port: 8032
      targetPort: http
