apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - common
  - guardrails
  - ekstraksi
  - structuring
  - scoring
labels:
  - pairs:
      app.kubernetes.io/part-of: nilam-ocr
    includeSelectors: false # selector Deployment tidak boleh berubah setelah dibuat
    includeTemplates: true # tapi pod harus membawanya: dipakai NetworkPolicy
